#pragma once

void convertToBin(int* binNumber, int number); // converts a decimal number to a binary number system

void printBin(int* number); // outputs to the console binary number

void binSum(int* firstNumber, int* secondNumber, int* sum); // sums two binary numbers

int convertToDec(int* number); // converts a binary number to a decimal number system